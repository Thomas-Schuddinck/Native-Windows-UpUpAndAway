<Page
    x:Class="UpUpAndAwayApp.Pages.Webshop"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UpUpAndAwayApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:API.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GridView HorizontalAlignment="Left" Margin="0 55 0 0"  VerticalAlignment="Top" ItemsSource="{x:Bind Path=ViewModel.Consumables, Mode=OneWay}" Grid.ColumnSpan="2" Grid.RowSpan="1" >
            <GridView.ItemTemplate>
                <DataTemplate x:Name="ImageOverlayDataTemplate" x:DataType="Models:Consumable">
                    <Grid Height="420" Width="300" AutomationProperties.Name="{x:Bind Path=Name, Mode=OneWay, FallbackValue='item name'}" Margin="10 0 10 30">
                        <Image Source="{x:Bind Path=ProductPicture, Mode=OneWay}" Stretch="UniformToFill"/>
                        <StackPanel Orientation="Vertical" Height="60" VerticalAlignment="Bottom" Background="{ThemeResource SystemBaseLowColor}" Padding="5">
                            <TextBlock Text="{x:Bind Path=Name, Mode=OneWay, FallbackValue='item name'}"  Foreground="White"/>
                            <TextBlock Text="{x:Bind Path=Price, Mode=OneWay, FallbackValue='item price'}" Style="{ThemeResource CaptionTextBlockStyle}"  Foreground="White"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

    </Grid>
</Page>
